Ты профессиональный Python-разработчик уровня Senior с обширным опытом разработки проектов на Django и PostgreSQL.

Задача проекта:
Создать веб-приложение, которое выполняет следующие функции:

1. **Импорт данных**:
   - Пользователь загружает CSV-файл, содержащий записи об абонентах: `id`, `ФИО`, `номер телефона`, `адрес`, `паспортные данные`, `дата рождения`.
   - Данные из CSV полностью импортируются в базу данных PostgreSQL.
   - Старые версии импортированных данных сохраняются отдельно с указанием даты и времени импорта. Реализовать механизм удаления старых данных через заданный интервал (например, через 30 дней).

2. **Интерфейс поиска**:
   - Реализовать веб-интерфейс для поиска данных абонентов по любому из полей (`id`, `ФИО`, `номер телефона`, `адрес`, `паспортные данные`, `дата рождения`).
   - Предусмотреть возможность комбинированного поиска (по нескольким полям одновременно).

3. **Система авторизации**:
   - Пользователи авторизуются с помощью email и пароля.
   - Внедрить дополнительную защиту через двухфакторную авторизацию (TOTP, Time-Based One-Time Password).

4. **Логирование действий пользователей**:
   - Логировать следующие действия пользователей: вход, выход, запросы поиска (с фиксацией параметров поиска).
   - Обеспечить доступность журналов логов для администратора системы через интерфейс или отдельный модуль.

Технические рекомендации:
- Используй Django ORM для работы с PostgreSQL.
- Проект структурируй согласно лучшим практикам Django (принцип MTV).
- Интерфейс реализуй на Django Templates и Bootstrap для простоты и удобства.
- Валидацию форм и обработку исключений сделай на высоком уровне.
- Используй библиотеки django-otp и django-two-factor-auth для TOTP.
- Используй Celery для периодических задач (удаление старых данных).
- Обеспечь высокую безопасность (защита от SQL-инъекций, XSS, CSRF).
- Напиши unit-тесты для основных функций.
- Применяй рекомендации PEP 8 и форматируй код с помощью black.

При реализации функций подробно комментируй и документируй код, чтобы он был понятен и легко поддерживаем.

